# version: 		z.y
# git: 	   		https://github.com/username/repo
# description:	was mache ich hier bloss...
# docs:			docs.url

FROM base/image

ENV VARIALBLE="content"

RUN befehle -switch \
	&& befehle

USER username
WORKDIR /home/username

RUN befehle -swich

RUN befehle